<Page x:Class="RevitReactionImporter.ControlInterfaceView"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      Title="RevitRxnImporterApp"
      Width="300"
      Height="250">

    <Page.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVis" />
    </Page.Resources>

    <Grid Background="White">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="25" />
            <RowDefinition Height="50" />
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0"
                    Orientation="Horizontal">

            <Button Name="ImportRAMReactions"
                    Width="60"
                    Height="60"
                    Margin="50 7 7 7"
                    HorizontalAlignment="Left"
                    VerticalAlignment="Top"
                    Click="OnImportBeamReactionsClick">
                <Image Source="/dev/RevitRxnImporter/RevitRxnImporter/Resources/import.jpg" />
            </Button>
            <Button x:Name="ResetBeamReactions"
            	Width="60"
            	Height="60"
            	Margin="60,7,7,0"
            	HorizontalAlignment="Left"
            	VerticalAlignment="Top"
            	Click="OnResetBeamReactionsClick" RenderTransformOrigin="2.427,0.448">
                <Image Source="/dev/RevitRxnImporter/RevitRxnImporter/Resources/reset.png"/>
            </Button>

        </StackPanel>
        <StackPanel Grid.Row="1"
                    Orientation="Horizontal">
            <TextBlock Name="ImportReactionsText"
                       Width="120"
                       Margin="20,0,7,7"
                       Text="Import RAM Reactions"
                       TextAlignment="Center" />
            <TextBlock Name="ResetBeamReactionsText"
                       Width="120"
                       Margin="0,0,7,7"
                       Text="Reset Beam Reactions"
                       TextAlignment="Center" />
        </StackPanel>

    </Grid>

</Page>
